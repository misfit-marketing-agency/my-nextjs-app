import { Metadata } from "next";

import ApproachMain from "./_components";

export const metadata: Metadata = {
  title: "Approach",
  description: "Approach | Misfit",
};

export default function ApproachPage() {
  return <ApproachMain />;
}
