import { Metadata } from "next";
import WorkMain from "./_components";

export const metadata: Metadata = {
  title: "Work",
  description: "Work | Misfit",
};

export default function Work() {
  return <WorkMain />;
}
